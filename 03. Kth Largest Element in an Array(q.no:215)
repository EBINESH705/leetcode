class Solution {
    public int findKthLargest(int[] nums, int k) {
        int n;
        int ans;
        n=nums.length;
        Arrays.sort(nums); 
        ans=nums[n-k];
        return ans;
    }
}
